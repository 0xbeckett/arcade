{
  "name": "arcade",
  "version": "1.0.0",
  "private": true,
  "description": "Phone-first Game Boy handheld shell + ASCII/dot-matrix LCD render framework + game plugin API",
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "watch": "bun run build.ts --watch",
    "typecheck": "bunx tsc --noEmit",
    "dev": "bun run build.ts && bun run serve.ts",
    "serve": "bun run serve.ts"
  },
  "devDependencies": {
    "playwright-core": "^1.61.1",
    "typescript": "^5.6.0"
  }
}
